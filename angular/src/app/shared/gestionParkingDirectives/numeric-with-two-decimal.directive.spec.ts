import { NumericWithTwoDecimalDirective } from './numeric-with-two-decimal.directive';

describe('NumericWithTwoDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericWithTwoDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
