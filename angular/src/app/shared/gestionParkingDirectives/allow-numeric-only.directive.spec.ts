import { AllowNumericOnlyDirective } from './allow-numeric-only.directive';

describe('AllowNumericOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowNumericOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
