import { StringToDateFormatPipe } from './string-to-date-format.pipe';

describe('StringToDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
